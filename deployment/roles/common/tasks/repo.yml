---
- name: Create repo directory
  file: 
    path: "{{ bridge_path }}"
    state: directory
- name: Synchronize the repository with current files
  synchronize:
    dest: "{{ bridge_path }}"
    src: ../../../..
    rsync_opts:
      - "--exclude=.git"
      - "--exclude-from=../.gitignore"
