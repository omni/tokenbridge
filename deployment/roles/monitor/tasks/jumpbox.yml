- name: Launch container
  shell: docker-compose up -d
  args:
    chdir: "{{ bridge_path }}/monitor"
- name: Initial checks 1/2
  shell: docker-compose exec -T monitor /bin/bash -c "node checkWorker.js"
  args:
    chdir: "{{ bridge_path }}/monitor"
- name: Initial checks 2/2
  shell: docker-compose exec -T monitor /bin/bash -c "node checkWorker2.js"
  args:
    chdir: "{{ bridge_path }}/monitor"
